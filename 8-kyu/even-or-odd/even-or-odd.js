// SOLUTION:
const evenOrOdd = number => number % 2 === 0 ? "Even" : "Odd";
​
// SOLUTION EXPLAINED:
// Use an arrow function with idenfier "evenOrOdd" that takes in the parameter "number"
// Then, if the number is divisible by 2, return "Even". Else, return "Odd".
