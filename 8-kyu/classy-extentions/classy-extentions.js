class Cat extends Animal {
  speak () {
    return `${this.name} meows.`
  }
}
​
/* SOLUTION EXPLAINED:
- use "extends" keyword
- use "this.name" keyword to return cat's name 
- use temporal literals to combine strings  */